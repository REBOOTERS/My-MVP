<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="home.smart.fly.httpurlconnectiondemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".HttpDemoActivity" />
        <activity android:name=".HttpUrlConnectionAcitivity" />
        <activity android:name=".VolleyDemoActivity" />
        <activity android:name=".Okhttp3DemoActivity" />
    </application>

</manifest>
